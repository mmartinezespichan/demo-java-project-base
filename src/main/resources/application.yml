server:
  port: 8090

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: demo-java

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true
